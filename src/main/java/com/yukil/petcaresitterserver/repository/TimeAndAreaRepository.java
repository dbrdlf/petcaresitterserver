package com.yukil.petcaresitterserver.repository;

import com.yukil.petcaresitterserver.entity.TimeAndArea;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TimeAndAreaRepository extends JpaRepository<TimeAndArea, Long> {
}
